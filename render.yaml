services:
  - type: web
    name: multiplayer-snake
    env: node
    plan: free # Or your desired plan
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: / # Root path should serve index.html 